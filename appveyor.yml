
version: "{build}"
skip_tags: true
clone_folder: c:\gopath\src\github.com\Aiicy\AiicyDS
clone_depth: 1

environment:
  GOPATH: c:\gopath
  GOVERSION: 1.7

deploy: false

before_build:
  - go get -v -t -d 

install:
  - go build -v
  
configuration:
  - Debug
  - Release

notifications:
  - provider: Email
    to:
      - admin@aiicy.com
    on_build_success: false
